{
  "manifest_version": 3,
  "name": "Cleanup Scripts",
  "version": "1.0.0",
  "description": "Load and execute JavaScript cleanup scripts (e.g. cookie consent removers) on pages matching a defined URI pattern.",
  "action": {
    "default_popup": "popup.html",
    "default_title": "Cleanup Scripts"
  },
  "permissions": [
    "activeTab",
    "scripting",
    "storage"
  ],
  "background": {
    "service_worker": "background.js"
  }
}
